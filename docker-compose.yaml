services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: arcadeportal
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production

      - ConnectionStrings__Default=Data Source=/data/arcade.db

      - Email__Host=mailpit
      - Email__Port=1025
      - Email__Username=
      - Email__Password=
      - Email__FromAddress=noreply@arcadeportal.pl
      - Email__FromName=ArcadePortal
      - Email__UseTls=false

    volumes:
      - db-data:/data

    depends_on:
      - mailpit

    restart: unless-stopped

  mailpit:
    image: axllent/mailpit:latest
    container_name: arcadeportal-mail
    ports:
      - "8025:8025"
      - "1025:1025"
    environment:
      - MP_MAX_MESSAGES=200
      - MP_DATA_FILE=/data/mailpit.db
    volumes:
      - mail-data:/data
    restart: unless-stopped

volumes:
  db-data:
  mail-data:
